class SVG {
    constructor(text, textColour, shape, shapeColour) {
        this.text = text;
        this.textColour = textColour;
        this.shape = shape;
        this.shapeColour = shapeColour;
    }

    render() { }

    setText() { }

    setShape() { }
}

module.exports = SVG